
# Primary upstream DNS provider, defaults to google DNS.
# Maybe it should be the one of your (wifi) router, if it provides dns entries
# for your local devices # (Digitalcourage):
DNS1=46.182.19.48

# Secondary upstream DNS provider (dns.as250.net; Berlin/Frankfurt):
DNS2=194.150.168.168

# IP on which pihole is listening for requests:
ServerIP=10.1.1.12

# Timezone is revlevant for the right time in the logs
TZ=Europe/Berlin

# Password for the web interface of pihole:
WEBPASSWORD=SuPeRseCreTpAsSwoRd


CONDITIONAL_FORWARDING=true
CONDITIONAL_FORWARDING_IP=10.1.1.1
CONDITIONAL_FORWARDING_DOMAIN=fritz.box
