version: '3'
services:
  airconnect:
    container_name: airconnect
    image: 1activegeek/airconnect
    networks:
      pubnet:
        ipv4_address: $IP_AIRCONNECT

networks:
  pubnet:
    external:
      name: pub_net
